// components/questions/MultipleChoice.ts
import React from 'react';

const MultipleChoice = ({ question, options, onAnswer }) => {
  return (
    // Render multiple choice question UI
  );
};

export default MultipleChoice;

// Implement similar components for other question types